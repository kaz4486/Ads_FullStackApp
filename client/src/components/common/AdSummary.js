import { Col, Container, Row, Button } from 'react-bootstrap';

const AdSummary = () => {
  // ad short version
  // tytuł, zdjęcie, lokalizacja, cena
  //
  //

  return (
    <Container>
      <h1>Ad summary</h1>
    </Container>
  );
};

export default AdSummary;
