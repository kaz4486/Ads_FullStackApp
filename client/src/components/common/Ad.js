import { Col, Container, Row, Button } from 'react-bootstrap';

const Ad = () => {
  // ad long version
  //all details
  //user details
  // jeśli jest zalogowany i to jego post - buttony Edit(przekierowuje do edycji) i Delete(przekierowuje na str. główną)

  return (
    <Container>
      <h1>Ad ze szczegółami</h1>
    </Container>
  );
};

export default Ad;
